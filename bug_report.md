---
name: Bug report
about: Something isn't working as expected
title: "[Bug] "
labels: bug
---

**Describe the bug**
A clear, short description of what's wrong.

**To reproduce**
Exact command you ran, e.g.:
```bash
python main.py -d "hello" -o test.png -s 0
```

**Expected behavior**
What you thought would happen.

**Actual behavior**
What actually happened — include the full error message/traceback if there is one.

**Environment**
- OS:
- Python version (`python --version`):
- `qrcode` / `Pillow` version (`pip show qrcode pillow`):

**Additional context**
Anything else relevant (screenshots, sample input, etc.).
